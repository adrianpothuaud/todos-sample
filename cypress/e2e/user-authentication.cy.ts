describe('As a guest user', () => {
  describe('When I open the app', () => {
    it('I am not logged in', () => {

    })
    it('I see the login form', () => {

    })
    it('I cannot login', () => {

    })
    describe('When I fill a random-unregistered email', () => {
      it('I see a loader', () => {

      })
      it('I am not logged in', () => {

      })
      it('I see the login form', () => {

      })
      it('I see not found error on the email field', () => {

      })
      it('I cannot login', () => {

      })
    })
    it('I see a link to registration form', () => {

    })
    describe('When I go to registration form', () => {

    })
  })
})

export {}
